Redeploy Edge Function `analyze-document`

1. Deploy the `analyze-document` edge function from the current repository code using the Supabase deployment tool.
2. Make zero code changes — only redeploy the existing function.
3. Confirm successful deployment.

No frontend, backend code, or database changes are required.